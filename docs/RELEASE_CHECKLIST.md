# Чек-лист выпуска

PROMPT 31 §24. Список того, что должно быть верно ПЕРЕД выкладыванием
очередной версии. Пункт считается пройденным только если его проверил
кто-то или что-то, а не «вроде бы так и есть».

Порядок не случайный: сначала машина проверяет то, что умеет, и лишь
потом человек тратит время на то, чего машина не умеет. Если падает
любой пункт из блока «Машина», к блоку «Человек» переходить незачем.

## Машина

Одна команда закрывает большую часть: `npm run verify`. Она выполняет
пункты 1–7 подряд и останавливается на первом упавшем.

| # | Пункт                              | Команда                        | Ожидание                       |
| - | ---------------------------------- | ------------------------------ | ------------------------------ |
| 1 | Реестр предположений сходится      | `npm run check:assumptions`    | маркеры и записи совпадают     |
| 2 | Линтер, включая границы слоёв      | `npm run lint`                 | ноль ошибок                    |
| 3 | Типы                               | `npm run typecheck`            | ноль ошибок                    |
| 4 | Модульные тесты                    | `npm run test`                 | все проходят                   |
| 5 | Сборка                             | `npm run build`                | успешна                        |
| 6 | Независимость от бренда            | `npm run check:brand`          | ноль упоминаний, ноль внешних обращений |
| 7 | Размер сборки                      | `npm run check:bundle`         | главный чанк ≤ 260 КБ gzip, `pdf`/`xlsx` отложены |
| 8 | Состав и содержимое пакета         | `npm run check:release`        | нет лишних файлов, секретов, отладочного вывода; манифест и воркер согласованы |
| 9 | E2E на собранном приложении        | `npx playwright test --project=chromium` | все проходят       |
| 10 | E2E dev-режима                    | `npx playwright test --project=chromium-dev` | все проходят   |

Два E2E-набора запускаются **по отдельности**: вместе они поднимают два
сервера и мешают друг другу (`ERR_CONNECTION_REFUSED`). Путь к браузеру
задаётся переменной `PLAYWRIGHT_CHROMIUM_PATH`.

`npx vitest run` **не проверяет типы**. Пропускать пункт 3 нельзя: без
него ошибки типов доезжают до сборки.

## Запреты

| #  | Пункт                                                            |
| -- | ---------------------------------------------------------------- |
| 11 | Ни один тест не отключён: `.skip` и `.only` в наборе отсутствуют  |
| 12 | Валидация не ослаблена ради прохождения тестов                    |
| 13 | Подтверждённые геометрические правила не менялись без доказанной ошибки |
| 14 | Ни одного неподтверждённого правила без записи в `docs/UNKNOWNS.json` |
| 15 | Не добавлено: ИИ, облако, аккаунты, подписки, платежи, реклама, аналитика, склад, поставщики, заказы, ЧПУ, BIM, новые редакторы, новые движки расчёта |
| 16 | Не добавлено: бэкенд, трекинг, обязательные сторонние сервисы     |
| 17 | Ни одной переменной окружения: продукт не должен требовать настройки |
| 18 | Порог размера не поднят ради прохождения проверки                 |
| 19 | `npm audit --omit=dev` — ноль уязвимостей в том, что попадает в браузер |

## Выкладка

Проверяется на настоящем хостинге после выкладки —
`docs/DEPLOYMENT.md` §7.

| #  | Пункт                                                                |
| -- | -------------------------------------------------------------------- |
| 20 | Сайт отдаётся по HTTPS: без него нет ни офлайна, ни установки          |
| 21 | Неизвестный путь отдаёт приложение, а не страницу хостинга (SPA-fallback либо `404.html`) |
| 22 | `index.html` и `sw.js` отдаются с `Cache-Control: no-cache`            |
| 23 | `/assets/*` отдаются с длинным кэшем: имена содержат хеш содержимого   |
| 24 | Манифест и `sw.js` доступны и отдаются с верным типом                  |
| 25 | Имя базы IndexedDB и ключи хранения не менялись без миграции           |

## Человек

То, чего автоматика не покажет. Проходится в настоящем браузере на
собранном приложении (`npm run preview`).

| #  | Путь                    | Что должно быть верно                                             |
| -- | ----------------------- | ------------------------------------------------------------------ |
| 26 | Новый проект            | Открывается сразу, без ожидания и без пустого экрана               |
| 27 | Создание шкафа          | Габариты, секции, ячейки, полки — каждое действие видно на сцене немедленно |
| 28 | Фасады                  | Дверь и ящики появляются, открывание правится, ручка на месте      |
| 29 | Конструкция             | Задняя стенка, цоколь, столешница, антресоль отражаются в деталировке |
| 30 | Производство            | Детали, чертёж, фурнитура, раскрой, готовность — числа сходятся между разделами |
| 31 | Планировщик             | Мебель ставится, привязывается к стенам и углам, не проваливается сквозь стены |
| 32 | Сохранение и открытие   | Проект переживает перезагрузку вкладки; поля шагов показывают структуру ОТКРЫТОГО проекта, а не единицы пустого |
| 33 | Экспорт                 | PDF и XLSX скачиваются, открываются, кириллица читается, чертежи на месте |
| 34 | Несохранённая работа    | Закрытие вкладки с правками предупреждает; без правок — молчит     |
| 35 | Отмена                  | Ctrl+Z и Ctrl+Shift+Z проходят по всей истории в обе стороны        |
| 36 | Телефон                 | 320 и 430 пикселей: горизонтальной прокрутки нет, лист параметров закрывается, шаги доступны |
| 37 | Только клавиатура       | Весь сценарий проходится без мыши; фокус виден всегда              |
| 38 | Приватный режим         | Приложение говорит, что хранилище недоступно, и не притворяется сохраняющим |
| 39 | Битый файл              | Импорт испорченного JSON объясняет причину и не роняет приложение   |
| 40 | Тёмная тема             | Все четыре экрана читаемы                                           |
| 41 | Установка приложения    | Chrome/Edge предлагают установку; значок и подпись верные; окно открывается без адресной строки |
| 42 | Работа без сети         | После первого открытия: отключить сеть, перезагрузить — приложение работает целиком, включая экспорт |
| 43 | Обновление версии       | После выкладки появляется «Доступна новая версия»; после нажатия встаёт новая, проекты на месте |
| 44 | Консоль                 | При проходе по всем экранам и экспорте — ни ошибок, ни предупреждений, ни битых запросов |

## Документы

| #  | Пункт                                                                 |
| -- | --------------------------------------------------------------------- |
| 45 | `docs/FINAL_DEFECT_LIST.md` отражает текущее состояние: ничего исправленного не осталось в `ACCEPTED`, ничего нового не пропущено |
| 46 | `docs/KNOWN_ISSUES.md` согласован со списком дефектов                  |
| 47 | `docs/UNKNOWNS.json` не содержит записей, которые уже подтверждены     |
| 48 | `docs/RELEASE_NOTES.md` описывает то, что есть, и не обещает лишнего   |
| 49 | `docs/THIRD_PARTY_NOTICES.md` совпадает с `package-lock.json` (команда в §6 документа) |
| 50 | `docs/PERFORMANCE_BUDGET.md` содержит текущие измерения, а не прошлые  |
| 51 | `README.md` описывает актуальное состояние продукта                    |
| 52 | Изменения поведения отражены в профильном документе, а не только в коде |

## Что НЕ является поводом откладывать выпуск

Чтобы чек-лист не превращался в бесконечный:

- **Пустой раздел присадки.** Правила не подтверждены источником;
  приложение честно говорит об этом и не выдумывает координат. Выпуск
  этим не блокируется — блокировался бы обратным: выдуманными
  отверстиями.
- **Неразмещённая деталь в раскрое.** Если деталь больше листа, верный
  ответ — сказать об этом, а не разрезать её самовольно.
- **Размер `pdf-lib`.** Он отложен и на первую отрисовку не влияет.
