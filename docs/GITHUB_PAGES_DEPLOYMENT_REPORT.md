# GitHub Pages Deployment Report

PROMPT 40 §18. Факты выкладки: что произошло, что проверено и что
проверить из этой среды оказалось нельзя.

## Сводка

| | |
| --- | --- |
| Repository | https://github.com/ArtemU1453/FurnityreBuilder |
| Branch | `claude/furniture-designer-audit-cjvvuf` (ветка по умолчанию) |
| Commit | `534b1fa` — выложен; цепочка `verify → build-pages → deploy` введена следующим коммитом |
| CI | **PASS** — прогон №40 и далее, все одиннадцать ворот |
| Build | **PASS** — сборка под базовый путь от `configure-pages` |
| Deployment | **PASS** — прогоны выкладки №4 и №5, `Reported success!` |
| GitHub Pages URL | **https://artemu1453.github.io/FurnityreBuilder/** |
| Assets | **PASS** — по составу артефакта и `check:release`; вживую не открыть |
| Application Load | **NOT VERIFIED** — из этой среды адрес недоступен |
| Create Project | **NOT VERIFIED** на боевом адресе · PASS на идентичном артефакте |
| Save / Reload | **NOT VERIFIED** на боевом адресе · PASS на идентичном артефакте |
| Production | **NOT VERIFIED** на боевом адресе · PASS на идентичном артефакте |
| PDF | **NOT VERIFIED** на боевом адресе · PASS на идентичном артефакте |
| XLSX | **NOT VERIFIED** на боевом адресе · PASS на идентичном артефакте |
| Deep Links | **NOT VERIFIED** на боевом адресе · PASS на сервере, ведущем себя как Pages |
| Rollback Strategy | **DOCUMENTED** — `docs/GITHUB_PAGES_DEPLOYMENT.md` |

## Почему половина строк — NOT VERIFIED

Не из осторожности и не из-за приложения. **Сетевой шлюз этой среды не
пропускает `artemu1453.github.io`:**

```
curl: (56) CONNECT tunnel failed, response 403
WebFetch: EGRESS_BLOCKED — Access to artemu1453.github.io is blocked
```

Тот же 403, что закрывает референсный сайт с самого начала проекта.
Открыть опубликованное приложение, щёлкнуть по нему и посмотреть на
экран отсюда невозможно — ни браузером, ни curl, ни через выборку
страницы.

Написать «PASS», не открыв страницу, было бы враньём того же сорта, что
и придуманный URL, от которого я отказался в PROMPT 37. Поэтому строки
разделены: что подтверждено — подтверждено, что нет — названо.

## Что подтверждено фактически

**Адрес.** Взят из вывода самой выкладки, а не составлен по шаблону:

```
Created deployment for 534b1faa2e5f2ba490948a154bd48ee936770727
Getting Pages deployment status...
Reported success!
Evaluated environment url: https://artemu1453.github.io/FurnityreBuilder/
```

**Выкладка состоялась.** Две подряд: прогоны №4 (`869f3e3`) и №5
(`534b1fa`), обе `success`. `has_pages: true` в настройках репозитория.

**Артефакт.** Выгружен `dist/` целиком и только он — 954 829 байт в
архиве, один файл `artifact.tar`. Ни исходников, ни `node_modules`, ни
тестов, ни конфигурации разработки.

**Целостность базового пути.** В той же задаче, до выгрузки, отработал
`npm run check:release`. Он сверяет базовый путь между разметкой,
манифестом и service worker'ом и отказывает при расхождении — а
расхождение здесь даёт ровно белый экран без сообщений.

## Что проверено на идентичном артефакте

Та же сборка, тот же базовый путь `/FurnityreBuilder/`, сервер, ведущий
себя как GitHub Pages (подкаталог, `404.html` на неизвестный путь, те же
типы содержимого):

| Проверка | Результат |
| --- | --- |
| Полный набор E2E | **157 пройдено**, 1 пропущен |
| Приёмка FLOW A–H | PASS |
| PWA, установка, работа без сети | PASS |
| Экспорт PDF и XLSX, в том числе без сети | PASS |
| Мобильные ширины 375 / 390 / 430 | PASS |
| Прямые адреса и неизвестный путь | PASS — отдаётся приложение |
| Внешние запросы | **0** из 8 |

Это не замена проверке на боевом адресе. Но это и не догадка: отличается
только сертификат HTTPS и поведение чужой площадки, а раскладка,
расчёты, хранение, офлайн и экспорт от адреса не зависят.

## Deep links

Отдельных адресов у экранов нет — навигация внутренняя
(`docs/DEPLOYMENT.md` §7). Единственный вопрос к Pages: что он отдаст на
неизвестный путь. Ответ — `404.html`, а он у нас копия входной страницы,
поэтому приложение откроется и там. Проверено на сервере с тем же
поведением; на боевом адресе — нет, по причине выше.

## Чего этот прогон не изменил

Публичный доступ к приложению — бесплатный, без регистрации, без оплаты,
без рекламы и трекинга. Проекты остаются на устройстве пользователя.
Внешних обязательных сервисов у продукта нет ни одного.

## Как проверить самому

Из среды с доступом к `github.io` весь набор направляется на боевой
адрес одной переменной:

```bash
PLAYWRIGHT_BASE_URL=https://artemu1453.github.io/FurnityreBuilder/ \
npx playwright test --project=chromium
```

Отдельного набора «проверок после выкладки» не заводится: разошёлся бы с
приёмкой при первой же правке.
