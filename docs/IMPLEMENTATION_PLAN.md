# План разработки

**38 этапов:** подготовительный 00 и основные 01–37. Каждый этап: цель · ограниченный объём · критерии готовности ·
тесты · ручная проверка. **После каждого этапа приложение остаётся работающим.**

Порядок скорректирован относительно предварительного списка задания.
Изменения и их причины — в §4.

---

## 0. ПРАВИЛА ВЫПОЛНЕНИЯ

1. Этап не считается завершённым, пока не выполнены **все** критерии.
2. Этап оставляет приложение запускаемым и пригодным к работе. Промежуточных
   «сломанных» состояний в основной ветке не бывает.
3. Любая неподтверждённая формула помечается `// ASSUMPTION(T-XXX-NN)` и
   регистрируется в `docs/UNKNOWNS.json`. CI отклоняет маркер без записи.
4. Геометрия покрывается тестами **до** появления соответствующего UI.
5. Новое движение проходит чеклист `MOTION_SYSTEM.md` §10.
6. Новый компонент реализует полную матрицу состояний `DESIGN_SYSTEM.md` §9.

---

## 1. ЭТАПЫ

### 00 — Снятие неопределённостей *(блокирует точность, не блокирует разработку)*
**Цель.** Провести 59 тестов из `PRIVETMAKET_FUNCTIONAL_SPEC.md` §2 при появлении
доступа к референсу, либо принять решения самостоятельно и зафиксировать их.
**Объём.** Заполнение `docs/UNKNOWNS.json`.
**Готовность.** У каждой позиции статус `confirmed` или `decided` с обоснованием.
**Тесты.** Нет (исследование).
**Ручная проверка.** Ревью реестра.
**Примечание.** Выполняется параллельно; этапы 01–08 от него не зависят
благодаря параметризации схем сборки.

### 01 — Инфраструктура
**Цель.** Каркас проекта с защищёнными архитектурными границами.
**Объём.** Vite + React 19 + TS strict; Vitest; Playwright; ESLint с границами
слоёв; Prettier; CI; `README`; `LICENSE`; `UNKNOWNS.json`; проверка маркеров
`ASSUMPTION`; проверка brand independence; локальный шрифт для PDF.
**Готовность.** `npm run build` проходит; импорт React в `src/geometry` роняет CI;
grep-проверка бренда в CI; пустое приложение открывается.
**Тесты.** Smoke-тест сборки; негативный тест правила границ.
**Ручная проверка.** Открыть страницу — виден каркас приложения.

### 02 — Доменная модель
**Цель.** Типы и конструкторы сущностей, ноль зависимостей.
**Объём.** `DATA_MODEL.md` §1–13 в коде; Zod-схемы; инварианты §15.
**Готовность.** Типы компилируются; инварианты проверяются; фикстура типового
шкафа создаётся и валидируется.
**Тесты.** Unit на инварианты; property-тест: любое валидное дерево проходит проверку.
**Ручная проверка.** Нет UI — ревью кода.

### 03 — Состояние и история
**Цель.** Стор документа, стор сессии, undo/redo на патчах.
**Объём.** Zustand + Immer patches; команды; коалесценция транзакций;
восстановление выделения при undo.
**Готовность.** 200 шагов истории; серия правок в одном фокусе = один шаг.
**Тесты.** Unit на историю; тест коалесценции.
**Ручная проверка.** Временная отладочная панель: применить команды, отменить, вернуть.

### 04 — Геометрия: каркас
**Статус: ВЫПОЛНЕНО (PROMPT 3).**
**Цель.** Три схемы стыка, боковины, верх, низ, внутренний объём.
**Объём.** `ARCHITECTURE.md` §5.3; формулы задокументированы в `docs/GEOMETRY_RULES.md`.
**Готовность.** Для W=1000 H=2000 D=500 T=16 каждая схема даёт документированные
размеры; сумма деталей согласуется с габаритом. Сверх исходного плана этапа
добавлены две вещи, полезные всем последующим этапам без переделки: аварийная
остановка конвейера при фатальной ошибке (недопустимый вход даёт пустой список
деталей и диагностику, а не геометрию со случайными координатами) и общий
bounding box (`GeometryResult.boundingBox`), нужный планировщику и рендереру.
**Тесты.** Снапшоты четырёх конфигураций (типовая, минимальная, крупная,
с утолщённым материалом) с явными числовыми проверками; property-тесты:
недопустимый вход всегда даёт пустой результат с ошибкой, допустимый —
только конечные положительные детали с уникальными id в границах габарита,
детерминизм; 24 явных граничных случая; круговой путь через сериализацию
не меняет результат геометрии; дымовой тест производительности.
92 теста в `tests/unit/geometry/` (было 19 после PROMPT 2).
**Ручная проверка.** Минимальная технико-проверочная панель в `src/app/App.tsx`:
поля габаритов, число деталей, внутренние размеры, bounding box, список проблем.
Не интерфейс конструктора — только проверка связки домен → геометрия → экран.

### 05 — Design system: токены и примитивы
**Цель.** Токены и базовые компоненты с полной матрицей состояний.
**Объём.** Цвет, типографика, отступы, радиусы, тени; кнопка, поле, селект,
вкладки, тумблер; светлая и тёмная темы.
**Готовность.** Все состояния реализованы; контраст проходит проверку;
хардкод цветов отсутствует.
**Тесты.** Тест контраста токенов; снапшоты состояний.
**Ручная проверка.** Страница-витрина компонентов; проверка с клавиатуры.

### 06 — Motion system
**Цель.** Пружинный движок с перехватом и передачей скорости.
**Объём.** Токены `MOTION_SYSTEM.md` §2; обёртки над Motion; хук reduced-motion;
утилиты `project()` и `rubberband()`.
**Готовность.** Анимация стартует от текущего значения; скорость передаётся;
reduced-motion работает глобально.
**Тесты.** Unit на `project()` и `rubberband()`; E2E на перехват анимации.
**Ручная проверка.** Демо: схватить летящий элемент — без рывка.

### 07 — Отрисовка схемы и viewport
**Цель.** SVG-сцена с зумом и панорамой.
**Объём.** Отрисовка `Part[]`, вид «фасад»; матрица viewport; резиновые границы;
hit-testing.
**Готовность.** Каркас из этапа 04 виден на экране; зум к курсору; панорама 1:1.
**Тесты.** Визуальный снапшот сцены.
**Ручная проверка.** Зум колесом и жестом, панорама, вписать по `0`.
**Примечание (PROMPT 4).** Отдельно от этого этапа, вне очереди, появился
минимальный ТЕХНИЧЕСКИЙ debug-рендер (`src/render/`, `DebugSchema.tsx`) —
он не является частью этапа 07 и не предвосхищает его: нет зума, панорамы,
резиновых границ, hit-testing, финального визуального языка. Он существует
только при `import.meta.env.DEV`, отсутствует в production-сборке и служит
исключительно для визуальной проверки геометрии (корпус, секции, ячейки,
размерные линии, id) во время разработки этапов 04–11. Подробности —
`docs/ARCHITECTURE.md` §10.1. Этап 07 как был не начат, так и
остаётся не начатым.

### 08 — Габариты: поля и прямое перетаскивание — ВЫПОЛНЕН (PROMPT 22)
**Цель.** Первое настоящее взаимодействие с изделием.
**Объём.** Панель габаритов; числовое поле без debounce; габаритные маркеры;
preview-пересчёт; контроллер жестов `INT-01`.
**Готовность.** Ввод в поле меняет схему на каждом символе; перетаскивание
маркера 1:1; один шаг undo на жест; 60 fps.
**Тесты.** E2E: ввод и drag; тест на отсутствие debounce; трассировка кадров.
**Ручная проверка.** Потянуть габарит — изделие перестраивается непрерывно.
**Сделано на PROMPT 22.** Поля габаритов правят модель без debounce; ручки
ширины и высоты на холсте ведёт `DragController` (захват указателя, порог
4 px, шаг по модификаторам, магниты, отмена по Esc). Во время жеста домен
не трогается — виден предпросмотр; команда `SetDimension` отправляется
один раз, на отпускании, поэтому один жест — один шаг undo. Правила —
`docs/EDITOR_INTERACTION.md`.
**Осталось.** Preview-пересчёт (показ будущей деталировки во время жеста)
и трассировка кадров как автоматический тест — оба относятся к этапу 30
(производительность).

### 09 — Дерево секций и раскладка
**Статус: ВЫПОЛНЕНО (PROMPT 4).**
**Цель.** Деление на колонки и строки.
**Объём.** `SplitNode`, `resolveSizes`, режимы `fixed`/`flex`, раскладка ячеек
геометрическим движком (не отрисовка — она в App.tsx debug-панели, полноценная
интерактивная схема остаётся планом, §10.2 ARCHITECTURE.md).
**Готовность.** Деление на N частей; сумма ячеек + перегородки = внутренний
размер (проверено property-тестом); секции, строки, колонки — все три формы
деления реализованы и покрыты явными тестами (Test 1, 3–9 в `layout.test.ts`).
**Тесты.** Property: точность суммы при любых входных данных (унаследовано из
PROMPT 2) + property на саму раскладку (`grid-properties.test.ts`); снапшоты
не заведены отдельно для сетки — покрыто явными числовыми проверками, что для
структурной геометрии информативнее непрозрачного снапшота.
**Ручная проверка.** Панель «Сетка» в `src/app/App.tsx`: задать строки/колонки,
нажать «Применить» — деление видно в debug-схеме и в счётчике «Ячеек».

### 10 — Перегородки: детали и перетаскивание
**Статус: ЧАСТИЧНО ВЫПОЛНЕНО.** Геометрическая половина (детали перегородок)
готова на PROMPT 4; интерактивная половина (drag, магниты, живые метки)
остаётся отдельной, ещё не начатой задачей — ниже разделено явно.
**Цель.** Ключевое взаимодействие конструктора.
**Объём.**
- *Готово:* детали перегородок — расчёт позиции, размера, роли
  (`partition`/`shelf-fixed`/`shelf-adjustable`), материала, детерминированного
  id (`layout.ts`, docs/GEOMETRY_RULES.md §9.5).
- *Не начато:* drag с точкой захвата; жёсткие границы при перетаскивании;
  магниты; две живые метки размеров во время жеста; замок `fixed`/`flex`
  как элемент управления на схеме (сам режим `SizeSpec` уже есть в домене
  с PROMPT 1, не хватает интерфейса для его переключения).
**Готовность (для оставшейся части).** 1:1 движение; стоп на минимуме
с объяснением; магниты работают; один шаг undo.
**Тесты (готово).** `layout.test.ts`, `grid-properties.test.ts`,
`stable-ids.test.ts` — стабильность id перегородки между пересчётами;
`partitions.test.ts` и `partition-properties.test.ts` (PROMPT 7) —
количество перегородок, их геометрия, вложенность секций и ячеек,
отсутствие пересечений деталей.
**Тесты (предстоит).** E2E-drag; тест границ; тест привязок.
**Ручная проверка (предстоит).** Тянуть перегородку — оба соседних размера
меняются синхронно.

### 11 — Полки
**Статус: ЧАСТИЧНО ВЫПОЛНЕНО (PROMPT 6).** Геометрия полки готова;
полкодержатели и перетаскивание — нет.
**Цель.** Наполнение полками.
**Объём.**
- *Готово:* равномерное (`placement.mode: 'auto'`) и ручное
  (`'manual'`) размещение; расчёт ширины, глубины, толщины, координат,
  роли (`shelf-fixed`/`shelf-adjustable`), материала, кромки,
  детерминированного id; проверка выхода за пределы ячейки и пересечения
  полок; отрисовка в debug-схеме отдельным цветом
  (`src/geometry/stages/fill.ts`, `docs/GEOMETRY_RULES.md` §14).
- *Не начато:* полкодержатели как фурнитура (этап 24, `HW-06`);
  перетаскивание отдельной полки (то же взаимодействие, что и drag
  перегородки из этапа 10, и появится вместе с ним).
**Готовность.** Количество и положение задаются двумя способами; размеры
корректны; полка не выходит за секцию и не меняет внешний габарит.
**Тесты (готово).** `fill.test.ts` — 15 сценариев задания PROMPT 6 §28 плюс
блок коллизий; `fill-properties.test.ts` — property на произвольные
W/H/D/число полок. Снапшоты отдельно не заводились: числовые проверки
для полки информативнее непрозрачного снапшота (то же решение, что
и для сетки на этапе 09).
**Тесты (предстоит).** E2E-drag полки; тест расчёта полкодержателей.
**Ручная проверка.** Панель «Сетка» в `src/app/App.tsx`: поле «Полок
в ячейке», кнопка «Применить» — полки видны в debug-схеме отдельным
цветом и в счётчике «Полок».

### 12 — Выбор и панель свойств — ВЫПОЛНЕН ЧАСТИЧНО (PROMPT 22)
**Цель.** Связь схемы и панели.
**Объём.** Одиночный, диапазонный и произвольный выбор; панель свойств,
раскрывающаяся от объекта; общие свойства при множественном выборе.
**Готовность.** Двусторонняя синхронизация схемы и панели; панель открывается
от источника.
**Тесты.** E2E на выбор; тест пространственного происхождения анимации.
**Ручная проверка.** Выбрать ячейки, изменить общее свойство.
**Сделано на PROMPT 22.** Одиночный выбор изделия, секции, ячейки и детали
указателем и с клавиатуры; инспектор, контекстный выделению, с действиями,
зависящими от состояния ячейки; переход от текста ошибки к объекту.
Правила — `docs/EDITOR_SELECTION.md`.
**Осталось.** Множественный выбор (`INT-08`, не подтверждён T-GRID-02) и
общие свойства при множественном выборе; пространственное происхождение
анимации панели.

### 13 — Задняя стенка — ВЫПОЛНЕН (PROMPT 14)
**Цель.** Варианты монтажа.
**Объём.** Накладная, в паз, отсутствует; сегментация; влияние на глубину.
**Готовность.** Каждый вариант даёт документированные размеры.
**Тесты.** Снапшоты трёх вариантов.
**Ручная проверка.** Переключить монтаж, увидеть изменение деталировки.
**Сделано на PROMPT 14.** Задняя стенка стала деталью (`Part` роли `back`,
этап `back`): три варианта монтажа со своими формулами рамки, разделение
по секциям с идентичностью по id секции, материал из Material Registry,
команда `SetBackPanel`, диагностика битой ссылки и известного ограничения
паза. Правила — `docs/GEOMETRY_RULES.md` §22, тесты —
`tests/unit/geometry/back-wall.test.ts`.
**Осталось.** Паз в деталях корпуса (непрямоугольная геометрия) и режимы
разделения `BY_WIDTH`/`CUSTOM` — оба не подтверждены.

### 14 — Материалы — ВЫПОЛНЕН ЧАСТИЧНО (PROMPT 13)
**Цель.** Пользовательская библиотека и назначение по ролям.
**Объём.** CRUD материалов; назначение; влияние толщины на геометрию; легенда.
**Готовность.** Смена толщины перестраивает изделие; предупреждение показано.
**Тесты.** Unit на пересчёт; E2E на смену материала.
**Ручная проверка.** Создать материал 18 мм, назначить корпусу.
**Сделано на PROMPT 13.** Реестр материалов (`MaterialLibrary`) стал
единственным источником материала и толщины детали:
`resolveEffectiveMaterial()` (`src/geometry/parts.ts`, правила —
`docs/GEOMETRY_RULES.md` §21) применяется полками, перегородками,
дверными створками и фасадами ящиков; добавлена категория `mirror`;
битая ссылка на материал даёт `MATERIAL_REFERENCE_BROKEN` (error), а не
тихую подмену; команды `UpsertMaterial`/`RemoveMaterial`/
`SetMaterialAssignment`/`SetDefaultMaterial` валидируются, отменяются и
сериализуются. Тесты — `tests/unit/geometry/materials.test.ts`,
`materials-properties.test.ts`, `tests/unit/state/material-commands.test.ts`,
E2E — `tests/e2e/debug-schema.spec.ts`.
**Осталось на этот этап.** Создание и удаление материалов из интерфейса,
декоры и текстуры, легенда материалов на схеме (`MAT-08`).

### 15 — Кромка — ВЫПОЛНЕН ЧАСТИЧНО (PROMPT 13)
**Цель.** Правила оклейки и влияние на раскрой.
**Объём.** Правило по умолчанию; ручная настройка сторон; `EdgeSizingPolicy`;
итог по погонным метрам.
**Готовность.** Переключение политики меняет размеры раскроя предсказуемо.
**Тесты.** Unit на обе политики.
**Ручная проверка.** Включить вычитание — размеры уменьшились на толщину кромки.
**Сделано на PROMPT 13.** Модель кромки (`EdgeSpec` — четыре стороны плюс
материал кромки) и `EdgeSizingPolicy` задокументированы как параметр
детали, а не отдельная деталь; зафиксировано правило «кромка не меняет
`Part.size` никогда, размеры раскроя — только при `subtractFromPartSize`»
(`docs/GEOMETRY_RULES.md` §21.5); материал кромки проверяется на битую
ссылку. **Осталось.** Редактор четырёх сторон в интерфейсе и итог по
погонным метрам (`MAT-05`, `MAT-06`).

### 16 — Валидация
**Цель.** Правила и панель проблем.
**Объём.** Правила `VAL-01…06`; панель проблем; переход к объекту; подсветка.
**Готовность.** Правило пролёта полки срабатывает; ошибки не блокируют
редактирование; блокируют только экспорт.
**Тесты.** Unit на каждое правило; E2E на переход к объекту.
**Ручная проверка.** Сделать полку 1200 мм — предупреждение появилось.

### 17 — Хранение — ВЫПОЛНЕН ЧАСТИЧНО (PROMPT 22)
**Цель.** Работа переживает закрытие браузера.
**Объём.** IndexedDB; автосохранение; список проектов; индикатор состояния;
режим при недоступном хранилище.
**Готовность.** Сценарий «создать → закрыть → открыть → продолжить» проходит.
**Тесты.** Integration с fake-indexeddb; E2E с перезагрузкой.
**Ручная проверка.** Закрыть вкладку и открыть заново.
**Сделано на PROMPT 22.** `ProjectRepository` подключён к экрану: явное
сохранение, четыре состояния в тулбаре и строке состояния, восстановление
последнего проекта при открытии вкладки, честное сообщение о недоступном
хранилище. Сценарий «изменить → сохранить → перезагрузить → продолжить»
проверен E2E.
**Изменено против плана.** Автосохранение (`PER-02`) **отклонено**:
молчаливая запись поверх сохранённого теряет работу, если пользователь
экспериментировал. Обоснование — `docs/ARCHITECTURE.md` §8.3.
**Осталось.** Список проектов (`PER-04`) и ревизии (`PER-03`).

### 18 — Импорт и экспорт проекта
**Цель.** Пользователь физически владеет своими данными.
**Объём.** Экспорт `.furniture.json`; импорт с валидацией; миграции; приём файла перетаскиванием.
**Готовность.** Круг «экспорт → очистка → импорт» даёт идентичный документ.
**Тесты.** Round-trip тест; тест отказа на битом файле.
**Ручная проверка.** Выгрузить, очистить хранилище, загрузить.

### 19 — Деталировка в приложении — РАСЧЁТ ВЫПОЛНЕН (PROMPT 19)
**Цель.** Пользователь видит производственные данные до экспорта.
**Объём.** Таблица деталей; группировка одинаковых; синхронное выделение со схемой;
сводка по кромке.
**Готовность.** Выделение детали в таблице подсвечивает её на схеме и наоборот.
**Тесты.** Снапшот деталировки реперного изделия.
**Ручная проверка.** Пройти по строкам, найти каждую на схеме.

**Сделано на PROMPT 19.** Расчётная часть: `calculateProduction(project)`
даёт деталировку с группировкой одинаковых деталей, сводку по кромке в
погонных миллиметрах, спецификацию фурнитуры, сводки присадки и раскроя и
централизованный список неподтверждённых правил. Технический вывод — в
панели разработчика.

*Не сделано намеренно:* таблица деталировки как интерфейс и синхронное
выделение со схемой — это финальный UI (§33 задания PROMPT 19).
Разбор — `docs/PRODUCTION_BOM.md`.

### 20 — Экспорт CSV и XLSX — XLSX ВЫПОЛНЕН (PROMPT 20)
**Цель.** Первые производственные файлы.
**Объём.** CSV с BOM; XLSX листами: детали, кромка, фурнитура.
**Готовность.** Файлы открываются в Excel и LibreOffice, кириллица корректна.
**Тесты.** Парсинг сгенерированного файла в тесте.
**Ручная проверка.** Открыть выгрузку в табличном редакторе.


**Сделано на PROMPT 20.** `createProductionXlsx`: семь листов без внешней
библиотеки — формат XLSX это ZIP из XML, и собственный писатель дал
побайтовый детерминизм и отсутствие транзитивных зависимостей. Числа
записаны числами, шапка закреплена, включены фильтры. Книга проверяется
в тестах сторонней библиотекой (`exceljs`, только devDependency).
Разбор — `docs/XLSX_EXPORT.md`.

*Не сделано:* отдельный CSV. XLSX покрывает ту же задачу и открывается в
тех же программах; заводить второй формат ради разделителя — работа без
пользы.

### 21 — Ящики
**Цель.** Полный расчёт ящика.
**Объём.** Направляющие; короб; дно; фасад; правило выбора длины; валидация глубины.
**Готовность.** Ширина короба соответствует зазору направляющей; длина выбрана
по ряду; ошибка при недостаточной глубине.
**Тесты.** Снапшоты; unit на подбор длины.
**Ручная проверка.** Поставить 3 ящика, проверить размеры.

### 22 — Фасады распашные
**Цель.** Двери с корректными зазорами и петлями.
**Объём.** Накладные и вкладные; число створок; сторона петель; количество петель;
предпросмотр открывания.
**Готовность.** Размер створки соответствует выбранной политике зазоров;
число петель — таблице.
**Тесты.** Снапшоты обоих режимов; unit на таблицу петель.
**Ручная проверка.** Две двери на шкаф, проверить зазоры в деталировке.

### 23 — Штанга, цоколь, столешница — ЦОКОЛЬ ВЫПОЛНЕН (PROMPT 14)
**Цель.** Оставшиеся элементы корпуса.
**Объём.** `HangingRod`; `BaseSpec`; `CountertopSpec` со свесами.
**Готовность.** Элементы появляются в деталировке и спецификации.
**Тесты.** Снапшоты.
**Ручная проверка.** Собрать гардероб со штангой и цоколем.
**Сделано на PROMPT 14 (цоколь).** Высота цоколя смещает корпус
(`resolveBasePlacement`), царги строятся деталями по явно заданному
составу, реализованы отступ и вырез, добавлены команды `SetBase`/
`UpdateBase` и правила валидации. Правила — `docs/GEOMETRY_RULES.md` §23,
тесты — `tests/unit/geometry/plinth.test.ts`.
**Осталось на этот этап.** Штанга (`GEO-11`), столешница (`GEO-13`) и
ножки как детали (`GEO-30`).

### 24 — Фурнитура и спецификация — ВЫПОЛНЕН ЧАСТИЧНО (PROMPT 16)
**Цель.** Полный комплект крепежа.
**Объём.** Правила количества крепежа; полкодержатели; ручки; PUSH;
сводная спецификация с трассировкой.
**Готовность.** Спецификация пересчитывается мгновенно; каждая позиция
прослеживается до породившей детали.
**Тесты.** Unit на правила количества.
**Ручная проверка.** Изменить глубину — число конфирматов изменилось по правилу.

**Сделано на PROMPT 16.** Появился слой `src/hardware/`: единый движок
`calculateHardware(project)` → `HardwareBOM`, один интерфейс `HardwareRule`
для всех видов фурнитуры, встроенный реестр определений поверх уже
существовавшего `HardwareLibrary`. Количество нигде не хранится и
пересчитывается вместе с геометрией; спецификация в файл проекта не
пишется. Считаются направляющие (две на ящик по типу), полкодержатели
(четыре на съёмную полку), ручки и push-механизмы (по одному на фасад).

*Не сделано намеренно:* количество петель и крепежа (петель, ручек, задней
стенки, корпуса) — ни один порог и ни один шаг референсом не подтверждён
(`T-DOOR-05`, `T-HW-03`, `T-HW-08`). Правила реализованы как интерфейсы:
вход посчитан, недостающая величина названа, число не выдумано. Полный
разбор — `docs/HARDWARE_RULES.md`. Ручная проверка «изменить глубину —
число конфирматов изменилось» до подтверждения `T-HW-03` невыполнима.

### 25 — Drag-and-drop наполнения
**Цель.** Прямое манипулирование наполнением.
**Объём.** Палитра; призрак с точкой захвата; подсветка цели; посадка с передачей
скорости; клавиатурная альтернатива.
**Готовность.** Перетаскивание работает 1:1; отмена по `Esc`; полный доступ с клавиатуры.
**Тесты.** E2E-drag; E2E-клавиатура.
**Ручная проверка.** Перетащить ящики в ячейку.

### 26 — Экспорт PDF: чертёж и деталировка — ВЫПОЛНЕН (PROMPT 20)
**Цель.** Главный производственный документ.
**Объём.** `pdf-lib` + локальный шрифт; чертёж с размерами; таблица деталей;
многостраничность.
**Готовность.** Кириллица извлекается из PDF; размеры соответствуют модели.
**Тесты.** E2E: извлечь текст из PDF и сверить; снапшот страницы.
**Ручная проверка.** Открыть PDF, проверить читаемость и размеры.

**Сделано на PROMPT 20.** `createProductionPdf`: титул со статусом
расчёта, общие размеры, материалы и кромка, спецификация деталей,
фурнитура, присадка, чертежи деталей с отверстиями, карты раскроя на A3,
неразмещённые детали и раздел «требует подтверждения». Кириллица — через
встроенный Liberation Sans из репозитория. Разбор —
`docs/PDF_EXPORT.md`.

*Отличие от плана:* извлечение текста из PDF в тесте не делается —
шрифт встраивается подмножеством с кодировкой Identity-H, и текст в
файле хранится индексами глифов. Вместо этого проверяются структура
документа (страницы, форматы, заголовок) и корректность встроенного
шрифта.

### 27 — Карта раскроя — ВЫПОЛНЕН ЧАСТИЧНО (PROMPT 17)
**Цель.** Раскладка деталей на листах.
**Объём.** Гильотинный алгоритм; ширина пропила; обрезная кромка; `grainLocked`;
процент использования; PDF-выгрузка.
**Готовность.** Детали не пересекаются и не выходят за лист; текстура не поворачивается.
**Тесты.** Property: отсутствие пересечений и выходов за границы при любом наборе.
**Ручная проверка.** Проверить раскладку глазами на реальном проекте.

**Сделано на PROMPT 17.** Появился слой `src/production/`: физические
детали превращаются в производственные позиции (`ProductionPart`),
группируются по материалу и толщине и раскладываются на листах
гильотинным алгоритмом с деревом свободных прямоугольников. Учтены пропил,
обрезная кромка, запрет поворота по текстуре; неразмещённые детали не
теряются, а получают причину. Есть технический рендерер карты
(`CuttingMap`) и параметры раскроя в настройках проекта
(`SetCuttingSettings`). Разбор — `docs/PRODUCTION_PARTS.md` и
`docs/CUTTING_RULES.md`.

*Не сделано намеренно:* PDF-выгрузка карты (этап 26), интерактивный
редактор раскроя и перетаскивание деталей (§36 задания). Не подтверждены
референсом ширина пропила (`T-CUT-01`), база процента использования
(`T-CUT-02`) и обрезная кромка по сторонам (`T-CUT-03`).

### 28 — Присадка — ВЫПОЛНЕН ЧАСТИЧНО (PROMPT 18)
**Цель.** Схемы сверловки.
**Объём.** Координаты отверстий по типам; система 32; выгрузка по деталям в PDF.
**Готовность.** Отверстия лежат в пределах детали; парные отверстия соединяемых
деталей совпадают.
**Тесты.** Property: совпадение координат в паре; отверстие внутри детали.
**Ручная проверка.** Сверить схему для одного узла вручную.

**Сделано на PROMPT 18.** Появился слой `src/drilling/`: единый движок
`calculateDrilling(project)` → `DrillingPlan`. Зафиксирована локальная
система координат детали и шесть её граней, реализованы переходы
локальные ↔ мировые, направление сверления, проверки границ, глубины и
пересечений, детерминированный порядок операций и технический вывод
карты присадки.

*Не сделано намеренно:* ни один технологический параметр не подтверждён
(`T-DRILL-01…05`), поэтому по умолчанию движок не создаёт ни одной
операции и называет недостающие величины. Правила петель, направляющих,
полкодержателей, крепежа, ручек и push-механизмов реализованы целиком и
заработают от заполнения параметров. Отдельно не хватает МОДЕЛИ СВЯЗЕЙ
между деталями (`PRD-06`): присадка полкодержателей, направляющих и
корпусного крепежа требует знать деталь-приёмник, а выводить её из
соседства запрещено. Разбор — `docs/DRILLING_RULES.md`.

### 29 — Доступность
**Цель.** Полное соответствие требованиям доступности.
**Объём.** Клавиатурный сценарий целиком; ARIA для схемы; `aria-live`;
все три медиа-настройки; масштаб текста 200 %.
**Готовность.** Сценарий 1 из `UX_FLOW.md` §17 проходится без мыши;
axe-проверка без нарушений.
**Тесты.** E2E-клавиатура; автоматическая проверка доступности; тест reduced-motion.
**Ручная проверка.** Пройти сценарий со screen reader.

### 30 — Производительность
**Цель.** Соблюдение бюджетов.
**Объём.** Профилирование; мемоизация; при необходимости — Web Worker
или гибридный Canvas-фон.
**Готовность.** Drag держит 60 fps на изделии из 120 деталей; пересчёт < 5 мс.
**Тесты.** Трассировка кадров в CI.
**Ручная проверка.** Работа на слабом устройстве.

### 31 — Мобильная и адаптивная раскладка
**Цель.** Полноценная работа на телефоне и планшете.
**Объём.** Sheet вместо панелей; жесты; плавающий тулбар; тач-цели.
**Готовность.** Сценарий 1 проходится на экране 390 px.
**Тесты.** E2E на мобильном viewport.
**Ручная проверка.** Реальное устройство.

### 32 — 3D-просмотр
**Цель.** Объёмное представление.
**Объём.** three.js; боксы из `Part[]`; орбита; **только просмотр**.
**Готовность.** 3D соответствует 2D-схеме деталь в деталь.
**Тесты.** Соответствие числа и габаритов боксов деталировке.
**Ручная проверка.** Сверить вид с чертежом.

### 33 — Планировщик
**Цель.** Расстановка мебели в помещении.
**Объём.** Стены; несколько предметов; drag с привязкой и резиновостью;
бросок с проекцией момента; размерные цепочки; поворот с шагом 15°.
**Готовность.** Предмет привязывается к стене; бросок работает; размеры до стен верны.
**Тесты.** E2E на привязку; unit на цепочки размеров.
**Ручная проверка.** Расставить три предмета в комнате.

### 34 — PWA и офлайн
**Цель.** Установка и работа без сети.
**Объём.** Service worker; манифест; собственная иконка.
**Готовность.** Приложение открывается офлайн и устанавливается.
**Тесты.** E2E в офлайн-режиме.
**Ручная проверка.** Отключить сеть, перезагрузить.

### 35 — Сквозные E2E
**Цель.** Все сценарии `UX_FLOW.md` §17 автоматизированы.
**Объём.** 8 сценариев; проверка сетевой изоляции.
**Готовность.** Все зелёные в CI; ни одного внешнего запроса.
**Тесты.** Полный набор Playwright.
**Ручная проверка.** Просмотр видеозаписей прогонов.

### 36 — UX-аудит
**Цель.** Проверка соответствия design standard.
**Объём.** Ревью каждого взаимодействия по чеклистам `MOTION_SYSTEM.md` §10
и `INTERACTION_MODEL.md` §10; замедленный просмотр анимаций; тест с людьми.
**Готовность.** Нарушений нет либо они зафиксированы с планом исправления.
**Тесты.** Визуальные регрессии.
**Ручная проверка.** Сессии с реальными пользователями.

### 37 — Финальный аудит
**Цель.** Готовность к публикации.
**Объём.** Повторный brand independence audit; проверка лицензий зависимостей;
сверка `UNKNOWNS.json` (не должно остаться `unknown` без решения);
проверка CSP; проверка бюджетов.
**Готовность.** Все чеклисты пройдены.
**Тесты.** Полный CI.
**Ручная проверка.** Финальное ревью.

---

## 2. КРИТИЧЕСКИЙ ПУТЬ

```
01 → 02 → 03 → 04 → 07 → 08 → 09 → 10 → 11 → 16 → 17 → 19 → 20
```

Тринадцать этапов до состояния «пользователь спроектировал шкаф с полками,
сохранил и выгрузил деталировку». Это минимально полезный продукт.

Далее по ценности: 21 (ящики) → 22 (двери) → 26 (PDF) → 24 (фурнитура) →
27 (раскрой) → 29 (доступность).

**Фактический прогресс на конец PROMPT 4.** Работа шла не строго по этому
порядку: выполнены этап 04 (каркас, PROMPT 3) и этап 09 (дерево секций
и раскладка, PROMPT 4) вместе с геометрической половиной этапа 10, а этапы
05–08 (design system, motion, полноценная интерактивная схема, габаритные
жесты) ещё не начаты — их заменял временный технико-проверочный UI
(`src/app/App.tsx`) и, с PROMPT 4, debug-рендер (`src/render/`). Это
осознанное отступление от критического пути ради проверки геометрического
движка тестами раньше интерфейса (правило §0.4), а не пропуск этапов.
Ближайшая нерешённая работа по самому критическому пути — этапы 05–08;
ближайшая по геометрическому движку — интерактивная половина этапа 10
(drag перегородки) и этап 11 (полки). Какой из этих путей выбрать
следующим — решение вне рамок PROMPT 4, требует новой инструкции.

**PROMPT 9 — архитектура наполнения ячеек.** Ревизия показала, что модель
наполнения существует с PROMPT 1: `LeafFill` — размеченное объединение на
листе дерева, и заводить рядом второй тип `Content` с полем `cellId`
означало бы превратить четыре структурные гарантии (нет битой ссылки, нет
сирот, нет переезда в другую ячейку, нет расхождения размеров) в четыре
обязанности. Вместо этого зафиксирован контракт: `resolveContentGeometry`
как чистая функция и явный статус `not-implemented` для видов без
геометрии — раньше ящики и штанга пропускались молча, и «модель есть,
деталей нет» ничем не отличалось от «ячейка пуста». Дверь оставлена
в `FacadeGroup`, а не перенесена в наполнение: фасад закрывает несколько
ячеек. Обувной модуль не заведён — его конфигурация не подтверждена
(`T-FILL-01`).

**PROMPT 8 — неравномерные секции и ряды.** Механизм размеров (`SizeSpec`
с режимами `fixed`/`flex`) существовал с PROMPT 1, но не был ни задействован,
ни покрыт тестами: все фабрики строили только равные `flex`-дети. Добавлено
недостающее: фабрика `createSizedSplit` для явных размеров, диагностика
`SPLIT_UNDERCONSTRAINED` (сумма меньше доступного места — до этого проходило
молча, с невидимым зазором у дальнего края), адресация `SetChildSize` по id
вместо позиции в массиве, показ диагностики движка в панели проверки (раньше
показывалась только диагностика слоя валидации, и ошибка расчёта оставляла
схему пустой без объяснения) и подписи объектов в debug-схеме. Этап 12 плана
(выбор и панель свойств) получил ещё одну готовую опору: размер конкретной
секции теперь меняется одной командой по её id.

**PROMPT 7 — вертикальные перегородки и независимые секции.** Ревизия
показала, что перегородки как детали и правило `partitionCount =
sectionCount − 1` уже реализованы с PROMPT 4, поэтому движок не
переписывался. Добавлено то, чего не было: секция как геометрическая
область с явными границами (`GeometryResult.sections` — до этого границы
секции восстанавливал сам рендерер, агрегируя ячейки), команда
`SetSectionCount` с сохранением идентичности существующих секций,
записанная политика идентичности (`docs/DATA_MODEL.md` §5.7), предикат
пересечения деталей `overlaps` с отличием касания от наложения и подписи
секций в debug-схеме. Этап 12 плана (выбор и панель свойств) от этого
ближе: у секций, ячеек и деталей теперь есть устойчивые id, на которые
выделение может ссылаться.

**PROMPT 6 — полки.** Выполнена геометрическая половина этапа 11: полка
стала настоящей деталью `Part` со стабильным id, а не математической
границей ячейки. Ячейки при этом не изменились: полки не создают новых
ячеек (`docs/GEOMETRY_RULES.md` §14.1) — это главное архитектурное решение
этапа. Этап конвейера `fill` помечен реализованным, но покрывает пока
только полки: `LeafFill`-виды `'drawers'` и `'rod'` он пропускает
до этапов 21 и 23.

**PROMPT 5 — ревизия правил расчёта корпуса.** Не новый этап и не изменение
статуса этапов 04/09/10: техническая ревизия уже реализованной геометрии
(«документация → код → тесты → фактическая геометрия»). Ни одна формула
каркаса или раскладки не оказалась неверной; исправлены только неточный
комментарий типа (`GeometryResult.bounds`) и покрытие тестами (ветка
`depthIncludesBackPanel = false`, end-to-end уравнение секций/строк из
задания §12). Найдена и явно задокументирована одна пограничная
неоднозначность модели (ширина секции при гипотетическом вложенном делении
по X — сейчас непостроимо ни одной фабрикой, но не запрещено моделью),
решение отложено до появления ручного редактирования дерева (этап 10,
интерактивная половина). Подробности — `docs/GEOMETRY_RULES.md` §13.

---

## 3. ГРУППИРОВКА

| Блок | Этапы | Результат |
| --- | --- | --- |
| Фундамент | 01–07 | Каркас, домен, геометрия, отрисовка |
| Ядро конструктора | 08–12 | Габариты, секции, полки, свойства |
| Конструкция | 13–16 | Задняя стенка, материалы, кромка, валидация |
| Данные пользователя | 17–20 | Хранение, обмен, первые выгрузки |
| Наполнение | 21–25 | Ящики, двери, фурнитура, прямое манипулирование |
| Производство | 26–28 | PDF, раскрой, присадка |
| Качество | 29–31 | Доступность, производительность, мобильный |
| Расширение | 32–34 | 3D, планировщик, офлайн |
| Завершение | 35–37 | E2E, UX-аудит, финальный аудит |

---

## 4. ОТЛИЧИЯ ОТ ПРЕДВАРИТЕЛЬНОГО СПИСКА ЗАДАНИЯ

| Изменение | Причина |
| --- | --- |
| Добавлен этап 00 (снятие неопределённостей) | Референс недоступен; 59 позиций UNKNOWN требуют явного управления |
| Хранение (17) и обмен (18) подняты выше экспорта в PDF | Потеря работы — худший исход для продукта без аккаунта. Сохранение важнее красивого чертежа |
| Деталировка в приложении (19) выделена в отдельный этап | Она — ядро ценности, а не побочный продукт экспорта; должна быть видна до выгрузки |
| Валидация (16) поднята до наполнения | Правила должны существовать раньше, чем появятся объекты, которые они проверяют |
| Design system (05) и motion (06) идут до любого экрана | Иначе примитивы придётся переделывать |
| Прямое манипулирование не выделено в один этап | Требование сквозное: каждое взаимодействие делается прямым сразу (этапы 08, 10, 25) |
| Отдельные этапы «основание», «верх», «столешница», «вылеты» объединены в 23 | Один механизм расчёта, дробить искусственно |
| Доступность (29) — этап, но требования соблюдаются с этапа 05 | Доступность нельзя добавить в конце; этап 29 — проверка, а не внедрение |
| Добавлен этап 34 (PWA) | Требование автономности от внешних сервисов логично завершается офлайн-режимом |
| Итого 38 этапов вместо 35 | Добавлены подготовительный 00, PWA (34), а объединение «основание/верх/столешница/вылеты» в 23 освободило место под 3D, планировщик и финальные аудиты |

---

## 5. ЧТО МОЖЕТ ПОЙТИ НЕ ТАК

| Риск | Этап | Митигация |
| --- | --- | --- |
| Формулы окажутся неверными | 04–22 | Параметризация схем; исправление = изменение константы |
| Preview-пересчёт не уложится в кадр | 08 | Деградация до контурного режима; порог для Worker зафиксирован |
| Карта раскроя даст плохой процент | 27 | Несколько стратегий; честная формулировка в UI |
| Присадка окажется сложнее оценки | 28 | Начать с одного типа крепежа (конфирмат), расширять |
| Кириллица в PDF | 26 | Локальный шрифт + тест извлечения текста |
| SVG-сцена станет тяжёлой | 30 | Порог 2000 узлов, гибридный Canvas |
| Мобильный потребует переделки взаимодействий | 31 | Pointer Events изначально едины для мыши и касания |
| Разрастание объёма | все | Жёсткие критерии готовности; функции вне этапа не добавляются |
